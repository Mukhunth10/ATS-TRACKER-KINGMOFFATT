-- Intentionally empty: generated by a failed `migrate diff` attempt (wrong
-- CLI flags) before the real migration in 20260731130000_candidate_facets.
-- Already applied and tracked — left as a no-op rather than risk a checksum
-- mismatch by deleting a recorded migration.
